<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guillaumemunsch.domothink">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.ConnectActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity"
            android:label="Main">
        </activity>
        <activity android:name=".activities.ConnectedObjectsActivity"
            android:label="Objets connectés">
        </activity>
        <activity android:name=".activities.SearchObjectsActivity"
            android:label="Recherche">
        </activity>
        <activity android:name=".activities.DirectivesActivity"
            android:label="Directives">
        </activity>
        <activity android:name=".activities.MyDomoBoxActivity"
            android:label="DomoBox">
        </activity>
        <activity android:name=".activities.StoreActivity"
            android:label="Store">
        </activity>
        <activity android:name=".activities.UpdateActivity"
            android:label="Mises à jour">
        </activity>
        <activity android:name=".activities.SettingsActivity"
            android:label="Paramètres">
        </activity>
        <activity android:name=".activities.CreateDirectiveActivity"
            android:label="Création d'une directive">
        </activity>
        <activity android:name=".activities.PluginDetailActivity"
            android:label="Détail d'un plugin">
        </activity>
    </application>

</manifest>

<Page
    x:Class="DomoThink.Views.Devices.EditDevicePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DomoThink.Views.Objects"
    xmlns:viewModel="using:DomoThink.ViewModels.Objects"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- Main grid -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical" Margin="12">

            <!-- Object panel -->
            <StackPanel x:Name="OBJECT_PANEL" Margin="0,0,0,8">
                <TextBlock x:Name="METHOD" x:Uid="TitleEditObject" Text="[Edit object]" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,8"  />

                <TextBlock x:Uid="ObjectName" Text="[Name]" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,4" />
                <TextBox x:Name="OBJECT_NAME" Text="{Binding ObjectInformations.Name, Mode=TwoWay}" Margin="0,0,409,0" />

                <TextBlock Text="Description" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,4" />
                <TextBox x:Name="OBJECT_DESCRIPTION" Text="{Binding ObjectInformations.Description, Mode=TwoWay}" Margin="0,0,409,0" />

                <Button x:Name="EDIT_BUTTON" x:Uid="EditObject" Content="[Edit object]" Margin="0,8,8,0" Command="{Binding EditObjectCommand}" Style="{StaticResource DomoButton}" />

            </StackPanel>
        </StackPanel>
    </Grid>
</Page>

<UserControl
    x:Class="DomoThink.Controls.DTileButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DomoThink.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Height="164" Width="164">
    
    <Grid>
        <Button BorderThickness="0" HorizontalContentAlignment="Left" HorizontalAlignment="Center"
                        Background="{StaticResource DPrimaryColor}" BorderBrush="{x:Null}" Height="164" Width="164" Command="{Binding Command}">

            <Button.Content>
                <Grid Height="129" Width="145">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.8*" />
                        <RowDefinition Height="0.2*"/>
                    </Grid.RowDefinitions>

                    <Grid Margin="0,0,0,0">
                        <Path Height="64" Stretch="Fill" Width="64" HorizontalAlignment="Center" VerticalAlignment="Center" Fill="#FFFFFFFF" Data="{Binding Icon}" />
                    </Grid>
                    <Grid Grid.Row="1" Margin="0,0,0,0">
                        <TextBlock Text="{Binding Text}" TextAlignment="Left" />
                    </Grid>
                </Grid>
            </Button.Content>

        </Button>
    </Grid>
</UserControl>
